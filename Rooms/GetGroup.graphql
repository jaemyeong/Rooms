query GetGroup($groupCode: String!) {
  group: GetStoreGroupByCode(param: {
    groupCode: $groupCode
  }) {
    ok
    error {
      code
      msg
    }
    data {
      list {
        _id
        name
        code
        zoneinfo {
          tz
        }
      }
    }
  }
}
