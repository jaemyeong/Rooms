query GetGroup($groupCode: String!) {
  group: GetStoreGroupByCode(param: {
    groupCode: $groupCode
  }) {
    success: ok
    error {
      code
      message: msg
    }
    response: data {
      stores: list {
        identifier: _id
        name
        code
        region: zoneinfo {
          timeZone: tz
        }
        products {
          identifier: _id
          name
          subtitle: subTitle
          code
          images
          informations: infos {
            identifier:key
            title: label
            message: content
          }
        }
      }
    }
  }
}
